class Location {
  String pid,locName, state, description, latitude, longitude, url, contact, address,imagename;
  Location({
  
    this.description,
    this.url,
    this.contact,
    this.address,pid,locName, state, latitude, longitude,imagename,
    
  });
}
